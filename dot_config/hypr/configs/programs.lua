-- Program definitions
--
-- Central place for program paths used throughout the configuration.

local programs = {
    terminal = "kitty",
    menu = "pkill wofi || wofi --show drun",
}

return programs
