#!/usr/bin/env bash
# Install i3 system-level packages (i3 and i3lock are in Nix).
# hash: {{ include "dot_config/i3/config.tmpl" | sha256sum }}

set -eu

sudo apt-get update
sudo apt-get install -y \
    xdg-desktop-portal
